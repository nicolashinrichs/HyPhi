# TODOs

*order by priority*

- [ ] Some functions are implemented twice
- [ ] more generally walk through all modules and scripts in `./code/hyphi/` and check naming, placing, what to expose to user, what not (use e.g., _private_function())
- [ ] Ricci Flow analysis script does not flow well with the current structure yet
- [ ] Create `CONTRIBUTION.md` file describing how to contribute to the project, coding style etc.
- [ ] Search for other "TODO"s in the repo files and resolve them
- [ ] Create a notebook as a user entry point (also for development) e.g., in `tutorials/`
- [ ] Unify the config loader framework for the whole project (this should be tested with the user notebook above)
- [ ] Adapt/add docstrings at the beginning of each Python script (what functionality is implemented here)
- [ ] Adapt/add docstrings to all functions and classes.
- [ ] Specify all dependencies with version in `pyproject.toml` (some missing deps are also mentioned in some scripts).
- [ ] Check the minimal Python version (the higher the better)
- [ ] Go through README.md files to check *placeholder text* in italics and fill in the corresponding information.
- [ ] The package version in the Citation section in the main README.md does not match the version at other places of the codebase. Ideally use a tool like bumpversion to keep this consistent across the codebase.
- [ ] Consider adding a citation request in the LICENSE file
- [ ] Consider how to keep the citation link up to date: Zenodo allows for DOIs not bound to a specific version). Consider how to add contributors to the citation
- [ ] Add proper `docs/` (e.g., with `mkdocs`)
