# =========================
# HyPhi — Pinned Dependencies
# =========================
# Core scientific stack
numpy>=1.24,<2.0
scipy>=1.10,<2.0
networkx>=3.0,<4.0
pandas>=2.0,<3.0
matplotlib>=3.7,<4.0

# Graph curvature (critical — pin to avoid API breaks)
GraphRicciCurvature==0.5.3.1

# Density estimation
KDEpy>=1.1,<2.0

# Entropy / information measures
infomeasure>=0.2,<1.0

# Parallel computation
ray>=2.0,<3.0

# Kuramoto simulations (JAX-based mean-field)
jax>=0.4,<1.0
jaxlib>=0.4,<1.0

# Delayed DDE Kuramoto (connectome model)
jitcdde>=1.8,<2.0
symengine>=0.11,<1.0

# EEG forward modelling (optional, for connectome notebook)
mne>=1.5,<2.0

# Circular statistics (used in CCORR demo)
astropy>=5.0,<7.0

# Configuration
tomli>=2.0,<3.0;python_version<"3.11"

# Progress bars
tqdm>=4.60,<5.0

# Statistics & machine learning (Phase 3)
statsmodels>=0.14,<1.0
scikit-learn>=1.3,<2.0

# Testing
pytest>=7.0,<9.0
